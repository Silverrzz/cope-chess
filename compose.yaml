x-cope-service: &cope-service
  build:
    context: .
  image: cope-chess:local
  restart: unless-stopped
  environment: &cope-environment
    COPE_DATABASE_URL: postgresql://cope@db:5432/cope
    COPE_DATABASE_PASSWORD_FILE: /run/secrets/cope_db_password
    COPE_DATABASE_POOL_MIN: "2"
    COPE_DATABASE_POOL_MAX: "50"
    COPE_DATABASE_POOL_TIMEOUT_S: ${COPE_DATABASE_POOL_TIMEOUT_S:-60}
    COPE_DATABASE_LOCK_TIMEOUT_MS: ${COPE_DATABASE_LOCK_TIMEOUT_MS:-5000}
    COPE_DATABASE_STATEMENT_TIMEOUT_MS: ${COPE_DATABASE_STATEMENT_TIMEOUT_MS:-120000}
    COPE_DATABASE_IDLE_TRANSACTION_TIMEOUT_MS: ${COPE_DATABASE_IDLE_TRANSACTION_TIMEOUT_MS:-30000}
    COPE_ADMIN_TOKEN_FILE: /run/secrets/cope_admin_token
    COPE_WEB_EVENT_TOKEN_FILE: /run/secrets/cope_event_token
    COPE_WEB_STREAM_URL: ws://web:8701/internal/stream
    COPE_WORKER_SERVER_URL: wss://${COPE_DOMAIN:?Set COPE_DOMAIN}/worker
    COPE_BENCHMARKER_SERVER_URL: wss://${COPE_DOMAIN:?Set COPE_DOMAIN}/benchmarker
    COPE_BENCHMARK_PREPARATION_TIMEOUT_S: ${COPE_BENCHMARK_PREPARATION_TIMEOUT_S:-1800}
    COPE_ENGINE_ARTIFACT_DIR: /var/lib/cope/engine-artifacts
    COPE_ENGINE_ARTIFACT_MAX_BYTES: ${COPE_ENGINE_ARTIFACT_MAX_BYTES:-1073741824}
    COPE_CLONE_TRANSFER_DIR: /var/lib/cope/clone-transfer
    COPE_CLONE_ALLOW_PRIVATE_ORIGINS: ${COPE_CLONE_ALLOW_PRIVATE_ORIGINS:-0}
    COPE_CLONE_EXPORT_TTL_HOURS: ${COPE_CLONE_EXPORT_TTL_HOURS:-72}
    COPE_CLONE_REMOTE_JSON_MAX_BYTES: ${COPE_CLONE_REMOTE_JSON_MAX_BYTES:-16777216}
    COPE_CLONE_TRANSFER_MAX_BYTES: ${COPE_CLONE_TRANSFER_MAX_BYTES:-10737418240}
    COPE_CLONE_UNCOMPRESSED_MAX_BYTES: ${COPE_CLONE_UNCOMPRESSED_MAX_BYTES:-42949672960}
    COPE_CLONE_ROW_MAX_BYTES: ${COPE_CLONE_ROW_MAX_BYTES:-16777216}
    COPE_CLONE_SOURCE_WAIT_TIMEOUT_S: ${COPE_CLONE_SOURCE_WAIT_TIMEOUT_S:-3600}
    COPE_CLONE_IMPORT_TTL_HOURS: ${COPE_CLONE_IMPORT_TTL_HOURS:-72}
  secrets:
    - cope_admin_token
    - cope_event_token
    - cope_db_password
  volumes:
    - engine-dockerfiles:/app/data/engines
  logging:
    driver: json-file
    options:
      max-size: 10m
      max-file: "5"

services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: cope
      POSTGRES_USER: cope
      POSTGRES_PASSWORD_FILE: /run/secrets/cope_db_password
      POSTGRES_INITDB_ARGS: --data-checksums
    secrets:
      - cope_db_password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    command:
      - postgres
      - -c
      - max_connections=${COPE_POSTGRES_MAX_CONNECTIONS:-300}
      - -c
      - shared_buffers=${COPE_POSTGRES_SHARED_BUFFERS:-512MB}
      - -c
      - effective_cache_size=${COPE_POSTGRES_EFFECTIVE_CACHE_SIZE:-2GB}
      - -c
      - wal_compression=on
      - -c
      - checkpoint_completion_target=0.9
      - -c
      - max_wal_size=${COPE_POSTGRES_MAX_WAL_SIZE:-4GB}
      - -c
      - idle_in_transaction_session_timeout=${COPE_POSTGRES_IDLE_TRANSACTION_TIMEOUT_MS:-60000}
      - -c
      - log_lock_waits=on
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cope -d cope"]
      interval: 5s
      timeout: 5s
      retries: 20
    stop_grace_period: 60s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"

  migrate:
    <<: *cope-service
    restart: "no"
    environment:
      <<: *cope-environment
      COPE_SERVICE_NAME: migrate
      COPE_DATABASE_STATEMENT_TIMEOUT_MS: ${COPE_MIGRATION_STATEMENT_TIMEOUT_MS:-1800000}
    command: ["db", "migrate"]
    depends_on:
      db:
        condition: service_healthy

  web:
    <<: *cope-service
    environment:
      <<: *cope-environment
      COPE_SERVICE_NAME: web
      COPE_UPDATE_REPOSITORY_URL: ${COPE_UPDATE_REPOSITORY_URL:-}
      COPE_UPDATE_REF: ${COPE_UPDATE_REF:-main}
    command: ["web", "--host", "0.0.0.0", "--port", "8701"]
    depends_on:
      migrate:
        condition: service_completed_successfully
    volumes:
      - engine-dockerfiles:/app/data/engines
      - engine-artifacts:/var/lib/cope/engine-artifacts
      - clone-transfer:/var/lib/cope/clone-transfer
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8701/health/ready', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 5

  scheduler:
    <<: *cope-service
    environment:
      <<: *cope-environment
      COPE_SERVICE_NAME: scheduler
    command: ["scheduler"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      web:
        condition: service_healthy

  worker-server:
    <<: *cope-service
    environment:
      <<: *cope-environment
      COPE_DATABASE_POOL_MAX: "150"
      COPE_SERVICE_NAME: worker-server
      COPE_WORKER_SERVER_STALL_TIMEOUT_S: ${COPE_WORKER_SERVER_STALL_TIMEOUT_S:-60}
    command:
      - worker-server
      - --worker-host
      - 0.0.0.0
      - --worker-port
      - "8702"
      - --poll-interval-s
      - "10"
      - --presence-flush-interval-s
      - "15"
      - --game-threads
      - ${COPE_WORKER_GAME_THREADS:-2048}
    depends_on:
      migrate:
        condition: service_completed_successfully
      web:
        condition: service_healthy

  clone-runner:
    <<: *cope-service
    environment:
      <<: *cope-environment
      COPE_SERVICE_NAME: clone-runner
      COPE_DATABASE_STATEMENT_TIMEOUT_MS: ${COPE_CLONE_STATEMENT_TIMEOUT_MS:-1800000}
      COPE_DATABASE_IDLE_TRANSACTION_TIMEOUT_MS: ${COPE_CLONE_IDLE_TRANSACTION_TIMEOUT_MS:-1800000}
    command: ["clone-runner", "--poll-interval-s", "1"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      web:
        condition: service_healthy
    volumes:
      - engine-dockerfiles:/app/data/engines
      - engine-artifacts:/var/lib/cope/engine-artifacts
      - clone-transfer:/var/lib/cope/clone-transfer

  updater:
    <<: *cope-service
    user: "0:0"
    command:
      - updater
      - --source-dir
      - /workspace
    environment:
      <<: *cope-environment
      COPE_SERVICE_NAME: updater
      COPE_UPDATE_REPOSITORY_URL: ${COPE_UPDATE_REPOSITORY_URL:-}
      COPE_UPDATE_REF: ${COPE_UPDATE_REF:-main}
      COPE_UPDATE_WORKER_WAIT_S: ${COPE_UPDATE_WORKER_WAIT_S:-1800}
      COPE_UPDATE_SERVICE_WAIT_S: ${COPE_UPDATE_SERVICE_WAIT_S:-180}
      COPE_UPDATE_ALLOW_ROLLBACK: ${COPE_UPDATE_ALLOW_ROLLBACK:-false}
      COPE_UPDATE_IMAGE_NAME: ${COPE_UPDATE_IMAGE_NAME:-cope-chess:local}
      COPE_UPDATE_COMPOSE_FILES: ${COPE_UPDATE_COMPOSE_FILES:-}
      COPE_COMPOSE_PROJECT: ${COPE_COMPOSE_PROJECT:?Set COPE_COMPOSE_PROJECT}
    volumes:
      - ${COPE_HOST_SOURCE_DIR:-.}:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
      - engine-dockerfiles:/app/data/engines
    depends_on:
      migrate:
        condition: service_completed_successfully
      web:
        condition: service_healthy

  benchmark-server:
    <<: *cope-service
    environment:
      <<: *cope-environment
      COPE_SERVICE_NAME: benchmark-server
    command:
      - benchmark-server
      - --host
      - 0.0.0.0
      - --port
      - "8703"
    depends_on:
      migrate:
        condition: service_completed_successfully

  caddy:
    image: caddy:2.10-alpine
    restart: unless-stopped
    environment:
      COPE_DOMAIN: ${COPE_DOMAIN}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ${COPE_HOST_SOURCE_DIR:-.}/deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      web:
        condition: service_healthy
      worker-server:
        condition: service_started
      benchmark-server:
        condition: service_started
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"

  backup:
    <<: *cope-service
    profiles: ["operations"]
    restart: "no"
    environment:
      <<: *cope-environment
      COPE_SERVICE_NAME: backup
    working_dir: /backups
    command: ["db", "backup", "--keep", "7"]
    volumes:
      - ./backups:/backups
    depends_on:
      db:
        condition: service_healthy

secrets:
  cope_admin_token:
    file: ${COPE_HOST_SOURCE_DIR:-.}/secrets/admin_token
  cope_event_token:
    file: ${COPE_HOST_SOURCE_DIR:-.}/secrets/event_token
  cope_db_password:
    file: ${COPE_HOST_SOURCE_DIR:-.}/secrets/db_password

volumes:
  postgres-data:
  caddy-data:
  caddy-config:
  engine-dockerfiles:
  engine-artifacts:
  clone-transfer:
